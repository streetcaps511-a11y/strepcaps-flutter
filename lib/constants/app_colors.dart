import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF030712); // Fondo oscuro
  static const Color secondary = Color(0xFF101829); // Barra superior
  static const Color accent = Color(0xFFFCC600);   // Amarillo dorado (botones, iconos)
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color background = Color(0xFF0F0F1A); // o el color oscuro que uses en tu diseño
}